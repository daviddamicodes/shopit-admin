import React from "react";
import "./Chart.scss";
import {
  LineChart,
  Line,
  XAxis,
  CartesianGrid,
  Tooltip,
  ResponsiveContainer,
} from "recharts";

const Chart = () => {
  return (
    <div className="chart">
      <h3 className="chartTitle">Sales Analytics</h3>
    </div>
  );
};

export default Chart;
